1.dialign museq.c 中没有extern void free_diag(struct diag* dg); 比较危险。
2.dialigan museq.c 中
int simple_aligner(struct seq_col *scol, struct diag_col *dcol, 
			    struct scr_matrix* smatrix, 
			    struct prob_dist *pdist, 
			    struct alignment *algn, int round)
与assemble.cpp中

char simple_aligner(struct seq_col *scol, struct diag_col *dcol, 
			    struct scr_matrix* smatrix, 
			    struct prob_dist *pdist, 
			    struct alignment *algn, int round)
声明不一致。
3. 1和2有可能导致意外链接

4.abyss-align ABSS PathConsensus link error
5.AdjList PopBubbles abyss-scaffold abyss-filtergraph complie error
6.查找所有OpenMP先关的编译选项工程。
7.可以使用#include <boost/math/special_functions/fpclassify.hpp> boost::math中的函数替换localmath
8.查看是否可以使用boost的getopt以及其他可以替换local的东西。
9.修改开始一些project的release settings
10.abyss-filtergraph 我猜需要改成boost::copy_if
